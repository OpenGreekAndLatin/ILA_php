# Intra Language Alignment tool (php implementation)

The tool allows automatic alignment between multiple texts in the same language, for the detection of textual variants and minor instances of re-use.
